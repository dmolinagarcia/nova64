# Decision log · open questions
> what was decided, why · what's left to close

The decisions shaping the current design, with their reasoning in one line — and the honest list of what's still open.

- D01 — ECP5 → **3× iCE40 TQ144** — BGA isn't hand-solderable; 0.5mm TQFP is. Accepted consequence: 128 Kbit EBR → HW walker + small TLB instead of on-chip tables.
- D02 — [[!superseded by D13/D15]] Main PSRAM **removed**; the 32 MB SDRAM is *the* memory — the QSPI gateware (the trickiest part) disappears, and a cache miss drops from ~µs to ~200–300 ns.
  NOTE: The removal stands and now covers the whole board, but its headline no longer holds: with the SRAM narrowed to 8 bits a fill is back in the µs range (D15). The decision survives on the gateware simplification alone.
- D03 — [[!superseded by D15]] External table SRAM **removed**; a single 2 MB SRAM (cache + tables + pinned) — ~35 pins that didn't fit in the TQ144.
- D04 — Amiga-style shared bus; out goes the dedicated FPGA-to-FPGA link and the eDP bridge — Neon taps the bus with the `$FE` window.
- D05 — Direct RGB-TTL 10.1” panel (ER-TFT101-1) — no bridge chip, no NDA (RTD2556 ruled out due to Realtek firmware). eDP/ANX6345 remains a future variant.
  NOTE: **This choice is size-bound, and that is the thing to know about it.** Parallel RGB-TTL exists only up to roughly 10 inches; from 14 inches upward every panel is a laptop panel speaking LVDS or eDP, and the direct path is simply gone (→ [Q23](sec_q#q23)).
- D06 — [[!superseded by D13/D35]] 512×300 → 1024×600 px-doubling — the framebuffer fits within the video PSRAM's budget and its QSPI bandwidth.
  NOTE: **Closed by [D35](sec_q#d35).** Px-doubling is no longer a property of the machine: it is **Mode 2b**, one of three video modes, with 2×2 replication filling the panel exactly. The GUI gets native 1024×600 in Mode 1 and games get replication in Mode 2, instead of every subsystem paying for one compromise (→ [T.33](sec_t#t33)).
- D07 — USB-only HID (RP2040's PIO-USB) — out go PS/2 and keyboard matrix. Capacitive GT911 touch: the resistive option needed 4 ADCs the RP2040 doesn't have free.
- D08 — SD as the only boot path; dedicated BIOS flash **removed** — incompatible with the pin budget after adding the SDRAM.
- D09 — Boosts (backlight, host VBUS) hanging off SYS, not VBAT — the machine runs on USB with the battery depleted. VPP 2.5V via BAT54: out goes the dedicated rail.
  NOTE: Both halves survive REV C, the second with its footing changed: the BAT54 now hangs off **3V3_MAIN**, which rises after the cores, so the iCE40's `VPP_2V5` ordering is satisfied by construction instead of being violated by it (→ [D20](sec_q#d20)).
- D10 — Full 2 KB paging per process (out goes the "one bank per process" model) — a tractable 32 KB flat table, decoupled from the cache's 256 B fill granularity.
- D11 — Hardware walker instead of a software trap on TLB miss — with 10 ns SRAM it costs ≈100 ns and simplifies the kernel.
- D12 — 32-bit PTE with a 16-bit frame from day one — 128 MB ceiling with no format migrations. 16 rather than the 15 bits the pool needs is free: the flags occupy exactly 16 bits, so the other half-word was going unused, and the clean split lets the walker read 32 bits and take the low half as address and the high half as permissions with no shifting or masking (→ [sheet K](sec_k)).
- D13 — PSRAM eliminated from the **entire board**, Neon's framebuffer included — every QSPI interface disappears with it. Neon gets its own 64 MB SDRAM instead: same part, same footprint, same controller as Helium's.
  NOTE: Streaming pixels across a link would need ~26 MB/s sustained with hard deadlines and would make Helium the bottleneck for the whole machine. A second SDRAM device costs the same pins as any smaller memory and buys double buffering at 16 bpp.
- D14 — SDRAM sized at **64 MB per device** (AS4C32M16SB-7TIN, 512 Mbit) — the ceiling of SDR silicon in volume; no 1 Gbit part exists. The 256 Mbit W9825G6KH stays as a drop-in fallback on the same footprint. **The second device is deliberately not populated**: at 8 MHz the 65816 sustains ~4 MB/s, so merely zeroing 64 MB takes ~16 s. A second /CS is routed to an empty footprint.
- D15 — SRAM narrowed to **1 MB ×8** with D0–D7 *and* A0–A10 shared with the CPU nets — 13 pins instead of 41, which is what makes the budget close at 90/107. Possible only because A0–A10 are the untranslated page offset (→ [F.2](sec_f#f2)). Accepted costs: Helium can no longer reach the SRAM alone, walks cost four accesses instead of two, and the shared nets carry three loads.
- D16 — Cache misses handled by **stalling PHI2 low**, not by ABORT — the W65C816S core is fully static and the WDC datasheet sanctions holding the clock indefinitely. ABORTB is retained for genuine protection faults only, which removes the frequent case from the most delicate path on the chip.
  NOTE: Clock gating was also preferred over the RDY pin for a specific reason: RDY is reported to be ignored during write cycles on some revisions, a 6502 NMOS inheritance. Gating the clock sidesteps the question — with PHI2 stopped the CPU is not in any state at all, rather than in one whose rules depend on the cycle type.
- D17 — The SRAM is a **hardware cache with tags**, never a tier of allocatable frames — migrating a page would force hardware to rewrite PTEs, which `mshare` and TLB coherence make impossible (→ [L.3](sec_l#l3)). 768 KB of 4-way cache in 2 KB lines with 256 B sub-blocks, plus 256 KB fixed and uncached.
- D18 — Debug access goes through a **debug agent inside Helium**, commanded by the RP2040 over a dedicated SPI chip select — the EC never masters the system bus. Pins (mastering needs ~32 lines the EC does not have), reuse (Helium already owns every memory controller) and coherence (an external master reads stale data past a dirty sub-block) all point the same way, and the agent's own registers stay **out of bank `$FF`** so the privileged path cannot be reached by the software it can bypass (→ [sheet R](sec_r)).
  NOTE: Helium also drives `BE` as part of this: the bus handoff has to be a synchronous transition inside one clock domain, not a race across the SPI link. Closes [Q25](sec_q#q25).
- D19 — **3V3 split into an always-on rail and a switched one** — the decision the rest of the power revision hangs from. A controller cannot switch the rail that powers it, so REV B's single 3V3 made EC power management impossible in principle rather than merely unimplemented. Accepted costs: one extra converter, and a domain boundary that has to be honoured net by net (→ [C.2](sec_c#c2), [C.13](sec_c#c13)).
- D20 — **1.2 V becomes a buck sourced from SYS**, not an LDO off 3V3. Two problems fall out together: the iCE40 wants VCC before VCCIO and an LDO off 3V3 guarantees the opposite, and dropping 3.3 → 1.2 V linearly at 250 mA burned ~0.5 W as heat in a SOT-23-5 on a battery machine. ~0.6 W recovered, and the ordering becomes structural rather than a firmware promise (→ [C.8](sec_c#c8)).
  NOTE: The rejected mitigation is worth recording so it is not proposed again: **holding `CRESET_B` low does not make a rail-order violation safe.** It prevents configuration, not conduction through the I/O clamps into an unpowered core rail.
- D21 — **MCP73871 → BQ25896 + CH224K**, and the pack to 2× 5000 mAh in parallel. The pack grew to ~10 Ah and linear charging at that current is not viable; a switching charger at 90–92 % with a programmable SYS floor and an integrated ADC also hands the EC rail telemetry for free. The CH224K negotiates **9 V** — not 12 V, which leaves no headroom under the charger's 14 V input OVP.
  NOTE: The charger's **OTG boost cannot replace the TPS61023** for the host port, and an earlier proposal that it could was wrong twice over: the boost drives VBUS, the same physical node as the USB-C input, and it only enables while unplugged (→ [C.5](sec_c#c5)).
- D22 — **Two level-held pins plus the existing SPI link** for the CPU↔EC power handshake: the pins carry attention, SPI carries the reason. A single wire encoding poweroff, reboot, ack and refusal by pulse width would be a bug source on a board where both endpoints are also under test. Costs 2 of Helium's spare I/O, onto a budget that does not close (→ [Q8](sec_q#q8), [S.2](sec_s#s2)).
  NOTE: `POWER_CTRL` is also the one register that departs from the "address as opcode" model of [M.2](sec_m#m2) — a small closed enumeration with no arguments is cheaper to decode by value than to spread over eight addresses. The departure is deliberate and confined to this block.
- D23 — **No `sys_poweroff()`.** Power management adds nothing to the syscall table; it is `/dev/power` and an `ioctl`, and the privilege check comes free from the device node's permissions rather than from a bespoke gate. A single-purpose syscall in a table otherwise built from general primitives would have been the first crack in the rule (→ [S.11](sec_s#s11), [J.4](sec_j#j4)).
- D24 — **Battery telemetry as double-buffered registers inside Helium**, staging → live → shadow, each copy a single cycle. Two cheaper designs were rejected: a **seqlock**, which moves retry logic onto the constrained CPU with no hard bound; and letting the EC **write telemetry into SRAM** over the debug agent's bus path, which would make battery reporting depend on the cache controller and the arbiter — the one thing the power subsystem is specified not to do (→ [S.16](sec_s#s16)).
- D25 — **The power registers sit outside the `DEBUG_ENABLE` gate.** Shutdown and battery reporting are production functions; gating them behind the debug strap would break them on every board built with debug disabled. They are read-only from SPI and cannot initiate a bus cycle, so the exception grants nothing the strap exists to withhold (→ [R.18](sec_r#r18)).
- D26 — **Neon is specified in full** — three video modes, a blitter, a command processor executing display lists out of its own SDRAM, and a resource budget that closes at 77 % of the device ([sheet T](sec_t)). It reconciles REV A against this board, which differs from what REV A assumed in four places: the reference clock, the SDRAM size, the existence of `NEON_BUS_BSY`, and where the interrupt goes.
- D27 — **Mode 0's text buffer and font live in Neon's block RAM, initialised from the bitstream** — not in video memory, and not loaded by anything. 8 KB plus 4 KB of the 16 KB available, which is expensive; it buys a machine that displays a correct screen the moment `CDONE` rises, with no SDRAM controller, no arbiter, no FIFO and no CPU in existence. **The console is therefore available as the debugging instrument for every subsystem built after it**, which is the whole argument (→ [T.1](sec_t#t1), [T.29](sec_t#t29)).
- D28 — **One clock domain inside Neon, at 103.125 MHz**, with the pixel stage on a clock enable every second cycle and `PCLK` out through a DDR register. No clock-domain crossing anywhere, so no synchroniser FIFO between scanout and memory and an entire class of intermittent failure removed. **The second PLL is freed** and has two claimants, audio and the VGA bring-up clock (→ [T.8](sec_t#t8), [Q54](sec_q#q54)).
  NOTE: REV A's 102.000 MHz is unreachable from this board's 25 MHz oscillator; 103.125 is the nearest legal PLL setting and the vertical total moves 635 → 640 to land back at ~60 Hz (→ [Q43](sec_q#q43)).
- D29 — **No hardware tile engine and no sprite engine.** The blitter redraws a full 320×200 tilemap in ~1.3 ms, under 8 % of a frame, and blits 32 masked 16×16 sprites in ~0.12 ms; hardware sprites exist to avoid save/restore of background on machines that cannot afford to redraw, and with double buffering and 64 MB that problem does not arise. **Saving ~1,650 LUT, about 21 % of the device** — which is what takes Neon from 95 % occupancy, not routable once audio grows, to 77 %. **The hardware cursor is retained**, because a GUI pointer must track the mouse independently of the rendering loop (→ [T.44](sec_t#t44)).
  NOTE: The trade is real and is recorded so it is not re-litigated. What is given up: sprites composited at scanout cost no framebuffer bandwidth and cannot be erased by drawing. What is gained: one rendering model instead of two incompatible paths, and the logic budget to fit the blitter, the command processor and audio on one device.
- D30 — **Blitter commands carry `(x, y)` coordinates, not 24-bit addresses.** Computing `base + y × stride + x` on the 65816 is a shift-and-add per object per frame on a CPU with no multiplier — precisely the work this architecture exists to remove. Neon does it instead, at one sequential multiply per command, ~16 clocks and ~80 LUT. The consequence is larger than the cause: **a destination becomes one 32-bit word, so moving an object is a single patch to an existing display list**, which is what makes per-frame CPU cost ~180 bytes instead of several milliseconds (→ [T.42](sec_t#t42)).
- D31 — [[!superseded by D36]] **Neon's registers live at `$FE:FF00`, inside the aperture, not in bank `$FF`.** Neon already decodes bank `$FE`; giving it a second window in the privileged bank would cost a decode in Helium and a wall on [Q22](sec_q#q22) for nothing.
  NOTE: The half of this that survives is the **palette**, which stays in the aperture at `$FE:8000` with 256 entries — it is bulk data, written a kilobyte at a time, and nothing about it is privileged (→ [T.49](sec_t#t49)). The half that was wrong is "for nothing": a second window buys the protection property of [D36](sec_q#d36), which is not nothing.
- D32 — **`PEEK` is deleted; the graphics aperture is readable.** REV A made it write-only, with a slow address/data register pair for diagnostic reads, on the premise that Neon could not stall the CPU. **`NEON_BUS_BSY` already exists** ([B.6](sec_b#b6)) and stalls PHI2 across the row activation, so a read simply works at a few hundred nanoseconds — about six times faster than the mechanism it replaces, and with no register file to build. Its protocol is [D37](sec_q#d37). **The software model is still written as though the aperture were write-only**, because nothing in the design needs to read pixels and anything that does will run at aperture speed (→ [T.15](sec_t#t15), [T.16](sec_t#t16)).
- D33 — **Backlight `EN` and PWM belong to the EC, not to Neon.** The EC is awake before Neon is configured and while the switched domain comes down, and it already owns every rail enable. Closes [Q6](sec_q#q6) and returns two pins to a budget that needs them (→ [T.52](sec_t#t52)).
- D34 — **Neon's interrupt goes to Helium's PIC, not to the CPU's `IRQB`.** Masking and priority come free from a controller that already exists, the CPU keeps one interrupt entry path, and it is one pin on each FPGA rather than a wire into the socket. Vblank, raster compare and the display list's `SIGNAL` are aggregated inside Neon and demultiplexed by the kernel reading `IRQ_STATUS` (→ [T.47](sec_t#t47)).
- D35 — **Px-doubling becomes Mode 2b.** What [D06](sec_q#d06) proposed as a global policy — draw at 512×300, emit every pixel twice — returns as one mode among three, with 2×2 replication filling the panel exactly. Mode 1 gives the GUI native 1024×600 at 1 bpp and Mode 2 gives games replication at 8 bpp, so no subsystem pays for another's compromise. Supporting 2b alongside 2a costs approximately nothing in logic: it is a register field driving the replication counters and the line stride (→ [T.33](sec_t#t33)).
- D36 — **Neon's registers live in bank `$FF`, at `$FF:8000`–`$FF:80FF`**, and bank `$FF` gains its first off-chip tenant. It reverses [D31](sec_q#d31), which had put them inside the `$FE` aperture, and it does so for a reason that outranks the tidiness argument on both sides: **`$FE` is user-mappable and `$FF` is not.** A process granted a framebuffer mapping could otherwise reach `LIST_BASE` and `CMD_CTRL.LIST_RUN` and aim the command processor — which reads and writes the whole 64 MB with no MMU in front of it — anywhere it liked. D31 could only answer that with a rule the mapping ioctl had to remember; **this makes it a property of the architecture**, putting Neon's control surface behind the same wall as the MMU's (→ [T.21](sec_t#t21), [J.5](sec_j#j5)).
  NOTE: **It costs no pins and no new mechanism.** Neon already taps A0–A15 and already captures the bank byte off D0–D7 during PHI2 low, so recognising a second bank is the same comparator with a different constant, and `NEON_BUS_SEL` simply broadens to mean "Helium has validated this cycle for Neon" across both windows (→ [B.6](sec_b#b6)).
  NOTE: **The base is `$FF:8000` so Helium's decode is one address bit** — "A15 set in bank `$FF` ⇒ not mine" — rather than a range comparison. Helium keeps `$FF:0000`–`$FF:7FFF` whole, so the block allocation [Q22](sec_q#q22) still owes can proceed with no risk of colliding with Neon later. The genuinely new gateware is small and specific: **Helium must release D0–D7 on a read in that window**, which is the same turnaround an `$FE` read already performs, and **Neon must gate on the strobe rather than on the address alone** — ABORTB cancels an instruction on the CPU, it cannot un-write a register on the far side of the bus. No stall is needed: the file is fabric registers and answers inside the PHI2 high phase (→ [T.23](sec_t#t23)).
- D37 — **The CPU can read VRAM, through a bus-busy stall — and the protocol is "stall first, ask afterwards".** `NEON_BUS_SEL` selects Neon's bus; while `NEON_BUS_BSY` is asserted Helium holds PHI2 **low**; when Neon deasserts it the data is in Neon's output register and Helium raises the clock. The alternative — Neon asserting a wait line fast enough to hold off the rising edge — is a race inside a ~62 ns low phase across an asynchronous inter-FPGA crossing, and it is unnecessary: **Helium decodes the bank itself, so it knows a read of `$FE` is in progress without being told.** It stalls unconditionally and the race disappears (→ [T.18](sec_t#t18), [B.6](sec_b#b6)).
  NOTE: **Stalling low rather than stretching the high phase is what keeps it cheap.** Extending the high phase would need a fresh datasheet reading on holding a static W65C816S with the clock high, which nobody has done; stalling low reuses the exact clock gate Helium owns for cache fills and the permission [D16](sec_q#d16) already established. It also falls out right on the bus, since D0–D7 carries the bank byte while PHI2 is low and Neon therefore must not drive then.
  NOTE: **Four requirements travel with the decision**, and the first would otherwise ship as an intermittent bug. **A read drains the write FIFO before it is issued** — writes are posted, so a read that overtakes a pending write returns the previous contents, failing according to arbiter load, which means "read back what was written" would pass almost every time. **The arbiter bounds the wait** — a row activation is ~60 ns and irrelevant, a read queued behind a whole blitter operation is up to 1.5 ms with the CPU clock stopped (→ [Q29](sec_q#q29)). **Helium's fill watchdog covers the path** (→ [F.11](sec_f#f11), [Q57](sec_q#q57)). And **the software model stays write-only**, because the restriction was doing real work: it is why save-under is a blitter copy and why assets flow one way (→ [T.19](sec_t#t19)).
  NOTE: **The same wire serves Helium with no clock involved.** When the debug agent reads VRAM through an external cycle, `BE` is low and there is no PHI2 to stall — Helium just waits for busy to clear before sampling D0–D7. `NEON_BUS_BSY` is a ready line that Helium *happens* to use to gate PHI2 when the requester is the CPU. And it is less slow than it sounds: a read loop runs at ~70 % of the speed of the same loop against SRAM, because the bottleneck is the 65816, not the SDRAM (→ [T.19](sec_t#t19)).
- D38 — **Neon's SDRAM is partitioned by function across its four banks**, and this is a first-order decision rather than an allocator preference. Banks 0 and 1 hold the front and back framebuffers, bank 2 the even-numbered window backing stores, bank 3 the odd ones plus the font atlas, icon sheets and audio buffers. **The invariant is that during composition the blitter's reads, its writes and scanout are always in three different banks**, so every ACTIVATE is issued inside another bank's data phase. Collapse it and delivered bandwidth halves, from ~160 to ~80 MB/s — a 2D blit crosses a stride at every line boundary, and a shared bank turns each crossing into a PRECHARGE plus ACTIVATE that overlaps with nothing (→ [U.16](sec_u#u16)). **Closes [Q50](sec_q#q50).**
  NOTE: Two obligations come with it. The **controller must map the bank bits to the high address bits**, or the partition cannot be expressed as an address range at all — the obvious alternative of interleaving banks on low bits to spread sequential traffic would destroy the whole policy. And **the kernel's VRAM allocator is no longer free to place a surface wherever it likes**; the partition is part of the interface, not an optimisation it may ignore.
- D39 — **Neon's arbiter is fixed-priority — refresh, scanout, audio, CPU, blitter — with preemption only at burst boundaries and a minimum burst of 8 words.** Scanout is not merely favoured but *shown* to be safe: worst case 23 clocks, **223 ns**, against a 512-word FIFO that a stall of that length drains by 5.8 words. This is the inverse of the Amiga's `BLTPRI`, which let the blitter starve the CPU; here the blitter can starve nothing. **Placing the CPU above the blitter is also what bounds [D37](sec_q#d37)'s read stall** — a read waits for one burst, never for a whole blit (→ [U.25](sec_u#u25), [U.27](sec_u#u27)). **Closes [Q57](sec_q#q57) and the Neon half of [Q29](sec_q#q29).**
  NOTE: The margin is the point rather than the number. If the clock falls to 51.5625 MHz under [Q44](sec_q#q44)'s fallback, if the burst grows, if audio's demand doubles, the stall stays two orders of magnitude inside the FIFO. **A real-time guarantee resting on an unverified estimate is not a guarantee**, and a scanout underrun has no degradation mode — the line is simply corrupt.
- D40 — **A prototype board is inserted before the target board**, and it is the first schedule decision this log has had to record. Helium and Neon are merged into one ECP5 on a commercial Colorlight i9 module with the CPU, SRAM, EC, VGA and audio on a carrier of our own, so the whole software roadmap runs on real gateware while the KiCad capture proceeds in parallel. **The blocked dependency was serial and is now two tracks**: [Q1](sec_q#q1)'s missing panel datasheet stops the schematic and stopped everything behind it, and it stops nothing on the carrier. Accepted cost: one board that is thrown away, and a discipline section — [P.12](sec_p#p12)–[P.18](sec_p#p18) — that has to be enforced at every merge or the prototype becomes a rewrite (→ [sheet P](sec_p)).
  NOTE: The reason the trade is worth taking is asymmetric, not marginal. A gateware defect found on the carrier costs a bitstream rebuild; the same defect found after E1.3 costs a board spin, and the parts of the design most likely to carry defects — MMU, cache, arbiter, blitter, compositor — are precisely the parts that need no board-specific silicon to exercise.
- D41 — **One i9 module, not two.** Two modules would give Helium and Neon separate SDRAM, which is the target topology, but each is locked at 8 MB with no expansion path and the pair doubles cost, carrier complexity and the inter-module wiring. One module reaches the same software milestones, and the topology difference is handled where it belongs — as a leak-table row and [P.15](sec_p#p15)'s memory-port abstraction, which is a discipline problem rather than a hardware one.
- D42 — **VGA through an R-2R ladder, not HDMI.** An earlier costing attributed an HDMI connector to the i9 extension board; it belongs to a Muse Lab board for the i5, a different product. The correction is an improvement rather than a loss: a TMDS encoder is 400–600 LUT of gateware that the target board deletes, whereas 6-bit-per-channel VGA is 20 pins from a budget with 24 spare and lands on **exactly the 18-bit palette entry the target panel takes** ([D05](sec_q#d05), [P.16](sec_p#p16)). One less thing built here that has to be unbuilt there.
- D43 — **The prototype carries the target's 1 MB ×8 SRAM, not a ×16 part.** An earlier costing for the carrier specified an IS61WV102416 with UB/LB. That re-widens what [D15](sec_q#d15) deliberately narrowed and would have the carrier rehearse a byte-lane topology the target does not have — while claiming, in the same paragraph, to be rehearsing the shared-net trick of [F.2](sec_f#f2) that only exists because the part is ×8. The AS7C38096A-10TIN of [F.5](sec_f#f5) goes on both boards.
- D44 — **Mode 0 geometry stays 128 × 32 on both boards; the prototype letterboxes instead of reflowing.** At 640 × 480 the console shows an 80 × 30 window into the same buffer, with [T.25](sec_t#t25)'s `TEXT_START + text_row*128 + column` unchanged; at 1024 × 600 all 128 columns fit exactly and the 32 rows sit inside a vertical border. **Making the geometry a synthesis parameter was the alternative and was rejected**: it adds a configuration axis the target never uses, and it forces the console driver and every scrollback consumer to ask the hardware how wide the screen is (→ [P1](sec_p#p1)).
- D45 — **The SPI-SD controller gets a register block of its own, with two data paths and a plain register file rather than address-as-opcode.** The card is unreachable from software until it has one, and the shape of it follows from what it has to carry: LBA, a physical destination and status are **state that persists between commands**, which is exactly the case [M.2](sec_m#m2)'s address-as-opcode convention does not cover — that convention applies to the four triggers, whose argument is a block count, and to nothing else. Both a **FIFO path** and a **DMA path** are specified, because the FIFO reaches [E6](sec_p#e6) without touching the arbiter or the cache and demand paging cannot use it: 512 bytes through the accumulator is ~440 µs of CPU at 8 MHz against 164 µs of SPI, and a FIFO-only design retracts [G.1](sec_g#g1)'s reason for putting the card behind Helium in the first place. Card initialisation stays in software behind a raw-command register, per [B.2](sec_b#b2) (→ [sheet G](sec_g)).
  NOTE: **The DMA path is what forces `CACHE_INVAL_FRAME` into [sheet M](sec_m)**, and that command is the part of this decision most likely to be skipped by whoever writes the gateware first. Helium acquires a second bus master writing SDRAM behind a write-back cache, and the dirty line left over the destination frame is not a staleness bug but a delayed overwrite of correct data (→ [M.12](sec_m#m12), [G.9](sec_g#g9)).
- D46 — **The expansion slot is a second instance of the inter-device contract, not a second bus.** A card gets `/EXP_SEL_n`, `/EXP_BSY` and `/EXP_IRQ`, [T.18](sec_t#t18)'s stall-first read protocol and [F.11](sec_f#f11)'s watchdog — the same shape Neon has, which is what [Q56](sec_q#q56) chose the names for. **One polarity is deliberately reversed**: `/EXP_BSY` idles *not busy*, because busy-by-default in a slot means a machine with nothing plugged in does not boot, and the race that polarity was defending against is killed instead by a mandatory stall window before Helium starts sampling. Cost: **three pins for the first slot, one for each after it**, with the bank byte demultiplexed by a '573 on the board and the slot buffered off the CPU nets by a '245 — both of which cost packages rather than FPGA pins (→ [sheet X](sec_x), [X.11](sec_x#x11)).
- D47 — **A card is reachable in two spaces and neither costs a pin.** Control is a 256-byte window in bank `$FF`, which inherits [L.10](sec_l#l10)'s keystone — no card in any slot is reachable from user code by construction — with Helium decoding the window and the card decoding inside it, exactly as [T.21](sec_t#t21) splits Neon's. Bulk is a **1 MB aperture per slot carved out of the frame numbers [F.10](sec_f#f10) deliberately leaves unpopulated**, so a PTE can name a card with the TLB and the permission bits still in front of it; the address reaches the connector on A11–A19, which Helium already drives and which are idle while the SRAM's `/CE` is ([F.2](sec_f#f2)). Aperture frames are non-cacheable and the gateware enforces it rather than trusting the PTE flag (→ [X.8](sec_x#x8), [X.9](sec_x#x9)).
- D48 — **No third-party bus mastering, and the reason is coherence rather than arbitration.** [M.12](sec_m#m12)'s rule — invalidate before a read into memory, flush before a write out of it — is honourable by Helium's own SD engine because Helium owns the cache, and is not honourable by a card somebody else designed. DMA stays a privilege of the northbridge; `/EXP_BR` and `/EXP_BG` are routed to the connector and left unconnected at the FPGA, in the same spirit as [F.10](sec_f#f10)'s unpopulated second `/CS` (→ [X.16](sec_x#x16)).
- D49 — **The slow tier is a third I2C bus with Helium as its only master, and its windows are mirrors rather than proxies.** `I2C-EXP` avoids multi-master arbitration on either existing bus — [C.13](sec_c#c13)'s R3 forbids a bus with two masters, not a third bus — and its two pins have been in [Q8](sec_q#q8)'s allocation since before the split left them without a device. A window cannot perform the transaction on demand: a byte over I2C is ~90 µs at 100 kHz against a 125 ns bus cycle, three orders of magnitude past what [M.6](sec_m#m6) permits to stall PHI2. So each window is a copy in block RAM refreshed by a sequencer, using **[S.16](sec_s#s16)'s staging → live → shadow mechanism with a device table in front of it** — the atomicity and staleness properties come along unchanged (→ [X.23](sec_x#x23), [X.24](sec_x#x24)).

## Open questions

- Q1 — [[!blocking]] ER-TFT101-1 module datasheet: backlight current/voltage, strap exposure on the FPC, GT911 connector, whether a panel reset or standby line is brought out to the FPC at all — **and whether the module needs any supply beyond 3V3 and the backlight string**. Most HX8282 designs generate AVDD, VGH and VGL on the module itself, and [sheet C](sec_c) assumes this one does: the panel appears in its tree as a single 200 mA tenant of 3V3_MAIN. If it does not, the power tree gains rails and that row is wrong ([C.17](sec_c#c17)). Order the panel in the **capacitive variant**. Blocks the KiCad schematic for sheets 1.5/1.6.
- Q2 — HDL choice (Verilog vs VHDL) — constrains the reference softcore (P65816 is VHDL).
- Q3 — Specific SDRAM controller among the three open-source candidates (adaptation and verification in E3).
- Q4 — EC↔kernel protocol — **closed for power**: shutdown, reboot and battery now have a specified transport, register set and failure behaviour ([sheet S](sec_s)). What remains under this heading is the **HID event path** and the RTC, neither of which travels the power path. Two things are owed, not one: the event *format* ([V.30](sec_v#v30) requires it fit 32 bytes and coalesce), and the *transport*, which [sheet H](sec_h) still records only as "the EC channel". The proposal on record is the EC as I2C master with Helium as a slave plus a dedicated `IRQ_HID` line — three pins already carried in Helium's budget — but it predates the REV C bus split, and Helium sits in the switched domain while [C.8](sec_c#c8) reserves the always-on bus to the charger, the gauge and the button. So it survives on **I2C-SW** or not at all, and that is a decision rather than an inheritance (→ [Q35](sec_q#q35), [Q41](sec_q#q41)).
  NOTE: **[D49](sec_q#d49) constrains this proposal without settling it.** Helium becoming a master on a bus of its own is precisely so that it never has to become a master on I2C-SW — which leaves the HID proposal's *EC master, Helium slave* arrangement intact, on the bus it was written for. What must not happen is the two being conflated later: `I2C-EXP` is Helium-as-master and I2C-SW is Helium-as-slave, and a single I2C block inside Helium serving both roles on two buses is a gateware decision nobody has taken (→ [X.22](sec_x#x22)).
- Q5 — Exact format of the BIOS → kernel info block.
- Q6 — **Closed — the RP2040.** Backlight `EN` and PWM stay with the EC, which is the only party awake both before Neon is configured and while the switched domain is coming down, and which already holds every rail enable through its expander. Neon recovers two pins its own budget needs and `BACKLIGHT_EN` leaves the register map (→ [D33](sec_q#d33), [T.52](sec_t#t52)).
- Q7 — **Closed, and in D04's favour.** There is no data link. Three *control* lines survive between the FPGAs and no more — `NEON_BUS_SEL` inbound, `NEON_BUS_BSY` outbound, and Neon's aggregated interrupt into Helium's PIC — and the 8 or 16 pins the old budget reserved for a link were reserved for something that does not exist. **They belong to the CPU bus tap Neon needs in order to see the `$FE` aperture at all**, which the same budget never counted; the substitution is recounted in [Q12](sec_q#q12) and it costs 10 pins net (→ [B.6](sec_b#b6), [T.51](sec_t#t51)).
- Q8 — [[!blocking]] The 17 free pins on Helium do not cover SPI-SD, the UART console and the debug port. Allocated today: inter-FPGA link 8, I2C 2, RP2040 handshake 3, spare 4. Sheets D and F both depend on interfaces that have no pins — the budget does not close as written.
  NOTE: **[Sheet X](sec_x) is the first thing to arrive at this question with an answer rather than a demand.** The expansion slot is priced at three pins and the slow bus at zero — its two are the "I2C 2" already in the allocation above, which the [C.13](sec_c#c13) bus split left without a device — and both are deliberately sized to be refusable: dropping the second slot is one pin, dropping the slot entirely is three, and neither costs a milestone (→ [Q81](sec_q#q81), [X.3](sec_x#x3)).
  NOTE: [Sheet R](sec_r) sharpens the deficit rather than easing it: the debug agent needs `DBG_CSN`, `DEBUG_ENABLE`, `BE` out of Helium and optionally `DBG_IRQN`, and it turns the CPU-side address and data pins bidirectional (→ [Q27](sec_q#q27)). It only shares `SCK`/`MOSI`/`MISO` with the configuration bus, which is the one economy available.
- Q9 — How many page tables actually stay resident? The 256 KB fixed region must hold the kernel (64 KB), the vectors, bank `$00` and the active tables at 32 KB each. K.2 used to promise 16 residents out of 512 KB; the honest figure now looks closer to four.
- Q10 — SRAM footprint compatibility: confirm in the datasheets that A20 on the 2 MB `AS7C316096` lands on an NC pin of the 1 MB `AS7C38096A` in TSOP-II 44. Usual for Alliance, unverified.
- Q11 — SDRAM column count: confirm the 512 Mbit part uses 10 column bits against the 256 Mbit part's 9, with the pinout otherwise identical — the premise the fallback footprint rests on.
- Q12 — **Recounted, and it closes at 100 of ~107** — but only after two errors in the old estimate cancelled each other, which is the part worth knowing. The old figure reserved **16 pins for an inter-FPGA link [D04](sec_q#d04) had already deleted**, and never counted the **26-pin CPU bus tap** that replaced it; the substitution costs 10 and the budget was wrong in the dangerous direction. Against that, VGA costs 2 dedicated pins rather than 14 (the R-2R ladders hang off the panel's own RGB bus), the backlight's 2 went to the EC ([Q6](sec_q#q6)), and the bank byte is captured off D0–D7 during PHI2 low instead of eight dedicated lines. Full table in [T.51](sec_t#t51). **18 bpp remains the recommendation and 24 bpp still does not fit.** What is left under this heading is narrower and is what still blocks sheet 1.5: sign-off against the real TQ144 pinout, I/O bank by I/O bank, with the SDRAM's 39 pins kept in banks that can meet 103 MHz and the panel's 22 kept together — and with the GBIN-capable pins read off Lattice's own TQ144 pinout rather than assumed, since the oscillator has to land on one in all three devices ([B.7](sec_b#b7)).
- Q13 — **Is the softcore even the right lever?** Given that memory and not the core is what caps this machine (→ [E.10](sec_e#e10)), effort may be better spent on the cache and memory subsystem than on Argon. Worth deciding explicitly before anything is invested in FPGA-C.
- Q14 — Argon's role and memory path — two halves of one question. Does the softcore *replace* the physical W65C816S on the shared bus or *coexist* with it? And if populated, does it use the standard bus into Helium, with the same PHI2 and cache constraints as the physical chip, or a dedicated wider link into Helium's cache? Without the latter its advantage is marginal.
  NOTE: Related to [Q7](sec_q#q7): both ask what private FPGA-to-FPGA paths this board actually has.
- Q15 — The real iCE40 Fmax of a candidate core: synthesize P65816 stripped of its SNES bus-timing fidelity on the target part and read the placed-and-routed figure. Every speed estimate in [sheet E](sec_e) is guesswork until this number exists.
- Q16 — Base core: P65816 cleaned up and given a flat memory interface, for maximum compatibility, versus FT816 for speed first — pending an instruction-coverage audit of FT816. Tied to [Q2](sec_q#q2), since P65816 is VHDL.
- Q17 — Freeze the canonical user virtual map *within* `$02–$FD`: where `.text` ends and `.data` begins, stack size, heap start.
  NOTE: The bank boundaries themselves are settled and not part of this question — an early draft placing user `.text` at `$01:0000` was rejected against [L.10](sec_l#l10).
- Q18 — Bank `$00` — **settled in principle**: each process's stack pages are pinned for its whole lifetime, and the vectors, trampolines and kernel stack occupy one or two privileged pages mapped identically in every process (→ [L.11](sec_l#l11)). What is left is sizing: the exact split of those one or two pages between the three tenants, how large the kernel stack should be, and whether there is one shared kernel stack or one per process.
- Q19 — Audit Calypsi's calling convention and its direct-page pseudo-register usage. Two things fall out of it: the exact context-switch save set, and how syscall arguments are passed.
- Q20 — Specify the binary format header — field layout, magic value, versioning.
- Q21 — Shared read-only text: is libc's `.text` duplicated in every process, or mapped shared read-only across page tables? The PTE R/X bits already allow it and the memory win is free, but it puts page reference counting into the loader.
- Q22 — Bank `$FF` register map — **Helium's MMU block is specified** (→ [sheet M](sec_m)), the **power block now exists too** (→ [sheet S](sec_s)), and the **peripheral block has acquired its first tenant and its register set** — the storage block of [G.6](sec_g#g6), specified as offsets precisely because this question is open. All three want a 256-byte wall of their own. Still unassigned: three base addresses, and the timer and interrupt controller entirely. Keep the walls between blocks so the decode and the privilege check stay trivial.
  NOTE: **And the count has grown again, in a direction that argues for settling the map rather than extending it piecemeal.** [Sheet X](sec_x) asks for one wall per expansion slot — proposed at `$FF:7E00` and `$FF:7F00`, the top of Helium's half against Neon at the bottom of the other one — plus one for the slow bus's block. That is now **six blocks and a timer** in a bank whose allocation is a five-minute decision nobody has taken, and the two [sheet X](sec_x) adds are the only ones that can be moved for free, since no gateware and no header depends on them yet (→ [Q82](sec_q#q82)).
  NOTE: The debug agent claims no space here and must not be given any — its register file is SPI-only by decision [D18](sec_q#d18) (→ [R.4](sec_r#r4)).
  NOTE: **Neon is back on this list, and it is the reason to settle the rest of the map now.** Its registers occupy `$FF:8000`–`$FF:80FF` — the bank's first off-chip tenant, and **the one block Helium does not answer** ([D36](sec_q#d36)). The base was chosen so that Helium's test is a single address bit rather than a range comparison: everything at or above `$FF:8000` belongs to Neon, everything below is Helium's to allocate as it likes. So the walls this question asks for now have a natural outer boundary, and the timer, the interrupt controller, the peripheral block and the power block ([Q40](sec_q#q40)) can be placed in `$FF:0000`–`$FF:7FFF` in any order without ever colliding with Neon (→ [T.22](sec_t#t22)).
- Q23 — What a panel larger than 10.1” would cost. RGB-TTL dies above ~10”, so a 14” class panel means a **transmitter bridge** — an ANX6345, taking the parallel RGB Neon already produces and running eDP link training itself in hardware, configured over I2C by the RP2040 before the CPU leaves reset.
  NOTE: On iCE40 that is effectively the only route: driving LVDS needs a 7:1 output serializer and native eDP needs hard SerDes, and neither exists on this family — so the two alternatives an ECP5 would have had are both closed here. Knock-on costs: ~22 pins on Neon at 18 bpp against a budget already near its limit (→ [Q12](sec_q#q12)), a framebuffer and bandwidth budget to re-derive at the new resolution, a larger backlight boost than the PT4110 sizing in [C.6](sec_c#c6), and touch to re-specify — laptop panels are usually non-touch, which puts the GT911 of [D07](sec_q#d07) in question.
- Q24 — ASID width, still unfixed. It drives both EBR consumption per TLB entry and how often an ASID must be recycled — and every recycle carries a mandatory flush (→ [M.8](sec_m#m8)). Four bits (16 contexts) and six (64) have both been floated; validate against the EBR budget alongside the 32–64 TLB entries already planned.
- Q25 — **Settled** — how the RP2040 reaches Helium's control registers with the 65816 held in reset. Neither option as posed: not bus mastering, and not the configuration path either, but a **dedicated chip select onto the shared configuration SPI data lines**, driving a debug agent that performs the access from inside Helium. An internal write to `$FF:00xx` meets the same comparator a CPU store would, so [sheet M](sec_m)'s whole command model is exercisable with no CPU (→ [D18](sec_q#d18), [R.21](sec_r#r21)).
- Q26 — **How big is the EC's flash, and does the machine boot without a card?** This document says W25Q16 (2 MB), firmware only, with the SD as the sole boot path ([D08](sec_q#d08)). The schematic sheets specify a W25Q128 (16 MB) holding backup copies of all three bitstreams and the BIOS — which would let the machine cold-boot with no card in the slot. That is a capability, not a BOM line, and it has to be decided before sheet 1.2 is drawn.
- Q27 — [[!blocking]] Debug agent pins and the `BE` handoff, all of it ahead of schematic capture: route `BE` from Helium to the CPU socket; make Helium's CPU-side address and data pins `inout` and check the I/O bank and pin-count impact on the TQ144 sheet; allocate `DBG_CSN` on both ends; place the `DEBUG_ENABLE` strap with its pull and jumper footprint; place the 3-pin UART header; and decide whether `DBG_IRQN` is worth a pin or the console polls (→ [R.23](sec_r#r23), [Q8](sec_q#q8)). The turnaround count between `BE` and Helium driving must be derived from the W65C816S datasheet at 3.3 V, not guessed.
- Q28 — The debug agent's **SPI-to-core clock domain crossing**: synchroniser depth and the command handshake, specified formally rather than left to whoever writes the module. It also sets the real ceiling on the 16 MHz link. This is where subtle intermittent failures live, and they would be attributed to the memory system rather than to the tool observing it.
- Q29 — **Arbiter priority order** now that the debug agent is a client: where it sits against the CPU port, the fill engine and the walker. SDRAM refresh stays highest; the rest is a decision, and `ERR_ARB` exists because the agent has to give up rather than wait forever (→ [R.17](sec_r#r17)).
  NOTE: **[D45](sec_q#d45) adds a fourth client and it is the easiest of them to place.** The SPI-SD DMA engine moves 512 bytes at a time and is fed by a link three orders of magnitude slower than the SDRAM, so it can sit at the bottom of the order and lose every contest without ever becoming the bottleneck — but it needs a bounded give-up like the debug agent's, and `SD_STATUS.ERR_ARB` exists for the same reason `ERR_ARB` does (→ [G.10](sec_g#g10)).
  NOTE: **Neon's arbiter is now a second instance of this question, and there the answer is a hard bound rather than a preference.** A CPU read of the `$FE` aperture stops PHI2 until Neon answers ([D37](sec_q#d37)), so whatever the blitter is doing when the read arrives is added directly to interrupt latency — up to 1.5 ms if a full-screen keyed copy has to finish first. **The CPU read port needs high priority and the blitter must be preemptible at a bounded burst boundary**, and the resulting worst case has to be stated in microseconds, not left implicit (→ [Q57](sec_q#q57)).
- Q30 — **Trace buffer depth** against the EBR left after the TLB and the cache tags. 1024 records at ~48 bits is the proposal, roughly six blocks on the HX8K; it competes directly with the 32–64 TLB entries of [L.7](sec_l#l7) and with whatever [Q24](sec_q#q24) settles for the ASID width.
- Q31 — [[!blocking]] **The AON↔switched crossings audit**: a net-by-net list of every connection over the domain boundary, each checked against R1–R3 ([C.13](sec_c#c13)). It deserves a review pass of its own rather than being folded into general ERC, because it is the item on the power sheet most likely to produce a board-level bug that is hard to diagnose — an AON pull-up back-feeding a dead rail has no symptom on a meter and a very confusing one on a scope. Blocks schematic sign-off.
- Q32 — **BQ25896 behaviour when the input steps 5 V → 9 V mid-operation.** The cold-start path depends on it: the EC boots on the default 5 V contract and the CH224K then renegotiates underneath a running system. The part is designed for this; the datasheet confirmation has not been done.
- Q33 — **Measure 3V3_MAIN and 1V2 at E1 and revise the budget** ([C.17](sec_c#c17)). The SDRAM and VCCIO rows dominate the total and both scale with bus activity, so the TPS63020's ~45 % margin is only as good as two estimates. A measurement above the assumption forces a higher-current part, which is a BOM change, not a firmware one.
- Q34 — **TPS63900 availability and its actual Iq.** The AON rail is chosen on quiescent current alone, and the ~17 mW standby figure and the "the pack self-discharges before it flattens" claim both rest on it. The TPS63001 is the fallback, at a cost in standby drain that would have to be re-derived. Tied to the same BOM pass as CH224K versus its successor CH224A, the K being phased out.
- Q35 — **Where the PCF8563 RTC lives now that the I2C bus has split in two.** It wants to keep time while the machine is off, which argues the always-on bus; but that bus is otherwise reserved to the charger and gauge, and every device added to it costs standby current permanently. The alternative — RTC on the switched bus with its own backup cell — moves the cost to a battery and a footprint (→ [H](sec_h)).
  NOTE: **[X.22](sec_x#x22) adds a third option that did not exist when this was written, and it is the better version of the second one.** On `I2C-EXP` — Helium's own bus, switched domain, no EC in the middle — the PCF8563 keeps time from its backup cell whether or not anything is powered, because a crystal does not care who owns the bus; and the kernel reads the clock as a mirrored window instead of asking the EC for it. It costs the same cell and footprint as the switched-bus option and deletes a transport. It becomes window 0 of a tier that exists for exactly this kind of device.
- Q36 — **The three press durations against the BQ25896's own `QON` thresholds.** Short (< 1 s), long (~4 s) and the charger's hardware reset have to be separable by a user with one button. If the EC's 4 s collides with the charger's window, the EC's number moves — the charger's is fixed in silicon (→ [S.6](sec_s#s6)).
- Q37 — **EC policy on a repeated `NAK`**, and the related question of whether a battery-critical request may be declined at all. A kernel that refuses twice is either busy or broken and the EC cannot tell which; escalating too eagerly loses data, escalating too late loses the machine. The battery case has a short argument on one side: the battery does not negotiate. Kernel and EC firmware must agree, and today neither is written (→ [S.15](sec_s#s15)).
- Q38 — **Carving the always-accessible SPI region** — `EC_PWR_STATE`, `TELEM_CTRL` and the staging bank outside the `DEBUG_ENABLE` gate — without disturbing the debug agent's existing map ([R.18](sec_r#r18)). Gateware work, and the addresses proposed in [sheet R](sec_r) are proposals.
- Q39 — **The grace period and the EC watchdog are a matched pair and have no canonical home.** 3 s inside 10 s today, with ~4.4 s of margin. A grace period longer than the watchdog turns every shutdown into a forced one *silently*, since the two are indistinguishable from outside until someone loses data. Both numbers must live in one place, in a form both the kernel and the EC firmware build from (→ [S.15](sec_s#s15)).
- Q40 — **Base address of the power and telemetry block within bank `$FF`** — the 16-byte telemetry window plus `POWER_CTRL` and `POWER_STATUS`, behind their own 256-byte wall (→ [Q22](sec_q#q22)). Blocks the gateware decode and the kernel headers equally, and it is a five-minute decision that two independent pieces of work are waiting on.
- Q41 — **The MCP23017's domain, and whether it still absorbs what [D.6](sec_d#d6) gave it.** REV C adds an `EN` and a `PG` per switched rail plus two Helium handshake pins, and it splits the I2C into an always-on bus and a switched one. The expander must sit on the always-on side to drive an enable at all — which puts it on a bus the power sheet otherwise reserves to the charger and gauge, and puts its outputs across the domain boundary under R1 and R2. Recount before schematic capture.
- Q42 — **Reboot: warm or cold?** The EC re-runs the power-up sequence after the rails drop, which is a cold restart by construction. Whether the kernel needs to distinguish the two — and whether anything should survive a reboot that does not survive a poweroff — is unanswered, and the answer decides whether `REQ_REBOOT` is one bit or the start of a state machine.
- Q43 — [[!blocking]] **Neon's PLL constants and the final modeline.** `DIVR=0 DIVF=32 DIVQ=3` gives 103.125 MHz core and 51.5625 MHz pixel from the 25 MHz oscillator, and 1344 × 640 puts the refresh at 59.95 Hz — but **those blanking values are a self-consistent set, not figures from a datasheet**, and the module datasheet is the same one [Q1](sec_q#q1) is still waiting for. The pixel clock and the active geometry will not change; only the porches will, and with them the exact refresh. Blocks schematic sheet 1.5 and the timing generator equally (→ [T.4](sec_t#t4), [E0.4](sec_p#e04)).
- Q44 — [[!blocking]] **Timing closure at 103.125 MHz on the HX8K**, with the SDRAM controller and the blitter placed together. Controllers in this range are documented on the family; that is not the same as a placed-and-routed design with everything else beside it. The fallback is single-rate at 51.5625 MHz, halving peak bandwidth to ~103 MB/s — every figure in [sheet T](sec_t) halves and the architecture survives, but the margins do not. Synthesise a stripped controller early rather than discovering this at E5 (→ [T.10](sec_t#t10)).
- Q45 — **Neon's EBR relief, and it must not be chosen before synthesis.** The 32 blocks are fully allocated with zero margin. Two reliefs exist: **reduce the font to 128 glyphs** — full ASCII plus box-drawing, freeing 4 blocks, and it should be a synthesis parameter from the first day — or **multiplex the 16 text-buffer blocks into graphics use**, freeing 8 KB at the cost of address muxes and of destroying screen contents across a mode switch. **The font blocks must not be reclaimed under any scheme**: their bitstream initialisation is the entire mechanism behind [D27](sec_q#d27). Decide once the blitter's real EBR demand is known (→ [T.12](sec_t#t12)).
- Q46 — **Closed by [D36](sec_q#d36), and structurally rather than by a rule.** The question was how to keep Neon's register file out of a user framebuffer mapping, since a process reaching `LIST_BASE` and `CMD_CTRL.LIST_RUN` commands an engine that reads and writes all 64 MB with no MMU in front of it. The answer is that **the register file is no longer in the aperture**: it moved to `$FF:8000`, and bank `$FF` is unmappable to user space by construction ([L.10](sec_l#l10)). The aperture is now data all the way through and the mapping ioctl has nothing left to remember. What survives is one negative test at E8 — a process that asks for a mapping of every page it can name must still not reach a Neon register — kept because an architectural guarantee deserves a test precisely when nobody expects it to fail (→ [T.21](sec_t#t21), [J.5](sec_j#j5)).
- Q47 — **The console's device name, and the one requirement that comes with it.** The Neon text console and the serial line cannot both be `/dev/tty`; this document now calls the screen `/dev/con` and leaves the UART as `/dev/tty`, which is a naming decision, not an architectural one, and it should be ratified before the driver table is numbered. The requirement is not cosmetic: **the console driver must hold its own scrollback in system memory and repaint from it**, because a graphics mode may reclaim the text buffer's block RAM ([Q45](sec_q#q45)) and a mode switch and back can destroy the screen. Normal for a virtual console, expensive only if discovered late (→ [T.55](sec_t#t55)).
- Q48 — **Ratify the coordinate-based command format and budget what it costs.** Three items travel together: the sequential multiplier in the command processor (~16 clocks, ~80 LUT); the clip unit **rejecting a fully off-screen rectangle before issuing any memory traffic**, on which the off-screen parking idiom for unused list entries entirely depends; and the processor **sign-extending coordinates and clipping negatives**, so an object can hang off the left or top edge. None is optional and all three are cheap; they are open only because nobody has written them down as requirements on the gateware (→ [T.42](sec_t#t42), [T.43](sec_t#t43)).
- Q49 — **Does `COPY_EXPAND` read its 1-bpp source at a bit-granular x offset, or must glyph atlas entries be byte-aligned?** The answer decides whether a proportional UI font can be packed tightly in the atlas or must be padded to 8-pixel boundaries, which costs VRAM and, more importantly, decides the width-table format the GUI is written against. Cheap to answer now, disruptive to change once fonts exist.
- Q50 — **Who owns Neon's 64 MB.** Framebuffers, glyph and icon atlases, pre-rendered level buffers, save-under pools and command lists all live there and nothing partitions them today; the addresses in the worked examples are placeholders. Two constraints belong to whatever allocator emerges: source and destination of a copy should sit in **different SDRAM banks** so the controller interleaves them instead of paying a row activation per alternation, and command lists must not be moved while `LIST_RUN` is set (→ [T.14](sec_t#t14)).
- Q51 — **Command-list versioning between gateware and kernel.** The explicit word count in every command exists so an unknown opcode can be skipped rather than desynchronising the stream — a decision made precisely because the two will drift during development. What is missing is the other half: how a kernel discovers which opcodes the gateware it is talking to actually implements. `NEON_VER` is one byte and a table has to hang off it somewhere (→ [T.43](sec_t#t43)).
- Q52 — **How far the service port reaches: SDRAM, or block RAM only.** Full reach costs an arbiter port shared with the CPU write FIFO and perhaps 100 LUT, and buys bulk asset loading straight from the card at ~2.5 MB/s against the CPU's ~1.3, at no cost to the 65816. Block RAM only is simpler and still gives the boot display and the debug channel, which are the two things no milestone can do without. The bulk-load benefit is real but nothing depends on it (→ [T.46](sec_t#t46)).
- Q53 — **Verify Calypsi generates absolute-long addressing for `(volatile uint8_t *)0xFE0000`** without reloading a bank register on every access. Every line of C that touches Neon depends on it, and so does the honesty of the ~140 µs per-frame figure. It belongs with the calling-convention and direct-page audit already outstanding (→ [Q19](sec_q#q19), [T.62](sec_t#t62)).
- Q54 — **The VGA bring-up clock, and whether VGA survives at all.** 640×480 wants 25.175 MHz and the video domain gives 25.78 — 2.4 % fast, which most monitors tolerate and none is specified to. Neon's second PLL is free and could synthesise it exactly, but audio wants that PLL too — less badly than it looks, since grounding the DAC's `SCK` puts the master clock inside the PCM5102A (→ [T.9](sec_t#t9)). The prior question is whether VGA is still worth its 2 pins and its gateware **now that Mode 0 puts a working console on the panel itself at [E1.7](sec_p#e17)**, three stages before VGA was ever going to be useful (→ [T.9](sec_t#t9)).
  NOTE: **Audio's half of this question is now closed, and it closed harder than [T.9](sec_t#t9) expected.** The iCE40 PLL is integer-only, and neither 44.1 nor 48 kHz is reachable from 25 MHz at any setting — 441 and 3,125 do not factor into `(DIVR+1) ≤ 16` times a power of two. **The second PLL cannot produce an exact audio clock**, so audio has no claim on it at all rather than a softer one, and this question reduces to whether VGA is worth two pins and its gateware ([W.11](sec_w#w11), [Q77](sec_q#q77)).
- Q55 — **Per-window VRAM buffers with compositing — deferred, not rejected.** Each window owning a buffer and the framebuffer being produced by compositing in z-order removes the damage problem entirely: occluded content still exists, so moving a window is a recomposite rather than a repaint from application state. With 64 MB and ~143 MB/s it is affordable — ~15 KB per window on top of the 75 KB framebuffer — which is not true of most machines this size. It is materially more gateware and more software, and **it must not be attempted before the flat framebuffer and its damage model are working** (→ [T.57](sec_t#t57), [T.58](sec_t#t58)).
- Q56 — **Closed. The three inter-FPGA nets are named, and the naming carries the philosophy**: `NEON_BUS_SEL` (Helium → Neon) selects Neon's bus, `NEON_BUS_BSY` (Neon → Helium) holds the CPU while that bus is busy, and `NEON_IRQ` aggregates Neon's three interrupt sources into Helium's PIC. `VRAM_SEL` and `BWAIT` are retired — the first described less than half of what it does now that Neon has two windows, the second described a mechanism that was going to stretch the wrong signal. Polarities and pulled states are settled with them, including the one deliberate departure from [B.8](sec_b#b8): **`NEON_BUS_BSY` is busy by default**, because for that net the pulled state must be the safe one (→ [B.6](sec_b#b6), [D37](sec_q#d37)).
- Q57 — **The read-stall timeout, and the bound the arbiter has to guarantee** — one number and one policy, and neither has an owner. The watchdog window for a VRAM read must be long enough to survive a refresh plus a legitimate arbiter queue and short enough that a missing Neon is reported rather than merely felt; it now serves three clients rather than two — a fill, a Neon that never answers, and a card that asserts `/EXP_BSY` and dies ([X.12](sec_x#x12)) — which is an argument for settling one number rather than three. It belongs in the same place as the fill watchdog's, not beside it ([F.11](sec_f#f11)). The policy is [Q29](sec_q#q29)'s: the CPU read port needs priority high enough, and the blitter preemption granularity fine enough, that the worst case is microseconds and not the 1.5 ms of a full-screen keyed copy. **Both are cheap to decide and expensive to discover** — the failure mode is scheduler jitter that appears only under drawing load (→ [D37](sec_q#d37), [T.19](sec_t#t19)).

- Q58 — [[!blocking]] **Which blitter: operations or channels?** [Sheet T](sec_t) specifies an operation-based engine taking `(x, y)` commands from a FIFO or a display list; [sheet U](sec_u) specifies the Amiga four-channel engine with 256 bitwise logic functions and raw pointer-plus-modulo addressing. They are different machines, not different register maps: the minterm LUT cannot express a colour-key compare and the operation set cannot express XOR. **The decisive figure is setup cost** — ~30 stores, ~19 µs of CPU per blit, which is nothing for a compositor doing a few large blits and fatal for the 24-sprite frame of [T.59](sec_t#t59), where it is ~450 µs against the 140 µs budgeted for the whole frame. **The recommendation is [U.4](sec_u#u4)'s third option**: this datapath with sheet T's command layer generating the pointers, which is roughly LUT-neutral and keeps both. Decide before RTL; it reaches the kernel, the toolchain and both milestones (→ [U.1](sec_u#u1)).
  NOTE: **A third argument arrives from software and it is about capability, not cost.** The toolkit needs **XOR** — selection highlight, pressed states, the text caret, the focus rectangle, any rubber-band outline — and sheet T's operation set has no raster op at all. Without one, every one of those becomes a fill plus a keyed copy from an inverted duplicate: twice the traffic, twice the commands, and a visual language rewritten around it ([V.14](sec_v#v14)).
- Q59 — **Does the GUI run at 1 bpp or 8 bpp, and does Mode 1 survive?** [T.30](sec_t#t30) calls the 1-bpp barrel shifter "not optional" for a GUI; [U.10](sec_u#u10) observes that at 8 bpp on a 16-bit bus the shifter degenerates to a byte swap, **deleting ~400 LUT and buying 256 colours instead of two**. The costs are a 614,400 B framebuffer against 76,800 and 36.8 MB/s of scanout against 4.6 — both comfortable against 64 MB and ~150 MB/s. A **1024 × 600 × 8 bpp mode does not exist in sheet T** and every figure in sheet U assumes it. Whether it replaces Mode 1 or joins it decides what the window server is written against (→ [U.12](sec_u#u12)).
  NOTE: **It also decides every icon, and that is the expensive half.** [Sheet V](sec_v) is written for 1 bpp throughout — gray as dither, selection as invert, depth as a 1px frame and a shadow line — and redrawing that visual language for 256 colours is weeks of art rather than an afternoon of code. **Answer this before drawing a single icon**, which is the toolkit's own open item folded in here rather than asked twice ([V.13](sec_v#v13)). And 2 bpp, floated as a middle path, is the worst of the three on this device: the shifter argument is not linear, so 2 bpp pays most of 1 bpp's ~400 LUT and buys four colours instead of 256.
- Q60 — **Flat framebuffer with repaint, or backing stores with recomposition?** [T.55](sec_t#t55) recommends the flat model and defers backing stores ([Q55](sec_q#q55)); [U.31](sec_u#u31) recommends backing stores with damage-limited recomposition and shows the budget carries ~90 full-screen composites a second, so full recomposition is always available as a correct fallback. **The disagreement is downstream of [Q59](sec_q#q59)** — T argued against a 1-bpp screen where repaint is cheap, U against 8-bpp stores with 64 MB behind them. The consequences are opposite and neither is retrofittable: under one, occluded pixels are gone and the window manager repaints from application state; under the other they are preserved and a move is a recomposite. **Answer Q59 first.**
  NOTE: **[Sheet V](sec_v) is the software half of this question and it argues the same way U did**, from a different direction: with backing stores the OS gets *smaller* — `layers.library` and its cliprect arithmetic disappear, clients never receive refresh events, a hung client still displays, and the event set drops to ten. It also sharpens the cost. At 1 bpp a full recomposite is ~1.0 ms and the screen can be rebuilt fifteen times a frame; **at 8 bpp it is ~8 ms and a typical desktop at 1.8× overdraw consumes essentially the whole frame**, so damage-limited recomposition stops being an optimisation held in reserve and becomes the routine path from the first day (→ [V.11](sec_v#v11)).
- Q61 — [[!blocking]] **Block RAM no longer closes.** Sheet T is at 32 of 32 with zero margin ([T.12](sec_t#t12)); sheet U's tally adds **2 blocks for audio that T never counted** and **2 for the 8-deep snapshot queue against T's 1** for the command FIFO, so the combined design wants 34–35 before the blitter's burst buffers. The reliefs are known — a 128-glyph font frees 4 ([Q45](sec_q#q45)), and a 2-bpp cursor rather than 4-bpp gives back half a block ([U.29](sec_u#u29)) — but **what was insurance is now spent**, and it has to be decided before RTL rather than after synthesis (→ [U.38](sec_u#u38)).
  NOTE: A third claimant now exists and it is not vapour: `DRAW_GLYPHS` ([Q64](sec_q#q64)) wants a source buffer and is the single largest CPU saving available to the GUI. **It is blocked on this question, not on its own merit.**

- Q62 — [[!blocking]] **Where the window server lives, and it is decided by [D36](sec_q#d36) rather than by preference.** The first pass at [sheet V](sec_v) made `wserver` a user task owning the `$FE` aperture and called that the protection model. Half of it works — [J.5](sec_j#j5)'s ioctl maps the aperture, and pixels, atlases, palette and command lists are all reachable that way. The other half does not: `CMD_PORT`, `LIST_BASE`, `CMD_CTRL.LIST_RUN`, `DISPLAY_BASE`, `VRAM_PAGE` and the cursor position are in bank `$FF`, **which is unmappable to user space by construction**, so **command emission is a privileged act on this machine**. The recommendation is the split the glossary already draws: **the compositor is kernel code** — vblank handler, z-ordered surface list, top-level command list, swap — **and the window server is a user task** doing hit-testing, focus, drag, menus, decoration and client validation, reaching the hardware through a `/dev/neon` node and an `ioctl`, which adds no syscalls and keeps [D23](sec_q#d23) intact. **The prize for getting it this way round is real**: a compromised window server can corrupt what is on screen and nothing else, because it cannot aim a command processor that reads and writes all of VRAM with no MMU in front of it (→ [V.4](sec_v#v4), [V.16](sec_v#v16)).
  NOTE: One wrinkle travels with it, and it answers the aperture-addressing question from an angle that first pass did not expect. The aperture shows 32 KB at a time and `VRAM_PAGE` is privileged, so **a user-space server pays a syscall every time its writes cross a 32 KB boundary**. Reprogramming costs one byte store and no stall — the cost is the call, not the hardware — so the mitigations are to keep the list arena, string cache and decoration assets within few pages, and to let the ioctl take page *and* offset. If crossings dominate in measurement, list building moves into the kernel with the compositor: one module, not a redesign (→ [V.18](sec_v#v18)).
- Q63 — **Ports, signals and messages in the syscall table** — the first addition to [J.3](sec_j#j3)'s dispatch table since it was written, and it deserves the scrutiny [D23](sec_q#d23) applied to power. Five calls: `port_create`, `port_send`, `port_recv`, `task_wait`, `task_signal`, with **fixed 32-byte messages copied by the kernel** and bulk payloads through the `mshare` that already exists. They pass the test `sys_poweroff()` failed — general primitives, not one subsystem's verb in a syscall's clothes. **What must not happen is two blocking mechanisms**: [N.3](sec_n#n3) already has per-device blocked queues, and a process waiting on a device *and* a message would have no single place to wait. Fold the device queues onto signal bits so `task_wait` is the only blocking primitive, or do not add it (→ [V.28](sec_v#v28), [V.29](sec_v#v29)).
  NOTE: The cost to design against is **~12 µs per message** — ~100 cycles of kernel copy at 8 MHz. Fine for window notifications and input events, and the reason **motion coalescing is not optional**: never more than one `EV_MOUSEMOVE` per frame per client. It also puts a requirement on [Q4](sec_q#q4)'s outstanding HID event format — an event must fit 32 bytes and must carry **absolute position and a button bitmap**, because a format expressed as deltas and edges cannot be coalesced at all.
- Q64 — **`DRAW_GLYPHS`, and it is complementary to the string cache rather than a replacement for it.** One command taking `(font_base, glyph_w, glyph_h, string_ptr_in_sdram, count, dx, dy)`. Costed against an 80-character line: per glyph is ~1.5 ms of CPU emission, a cached composed string is ~18 µs but ~1.5 ms the first time, and `DRAW_GLYPHS` is ~80 µs **with no first time**. So **the cache wins for stable labels and `DRAW_GLYPHS` wins for text that changes** — the editor, the terminal, the file listing, which is what a desktop is for. A 128 × 32 console repaint is ~3.2 ms against ~76 ms per glyph. It also deletes 256 KB of strip and all the LRU machinery. Cost it in LUT and EBR against the 128-glyph font relief before committing to either (→ [Q61](sec_q#q61), [V.26](sec_v#v26)).
- Q65 — **Command-list fetch is a fifth arbiter stream and [D38](sec_q#d38) never counted it.** During a composite pass the command processor fetches from SDRAM while the blitter reads a backing store and writes the back buffer. For an even-numbered window those are three different banks and the invariant holds; **for an odd-numbered window the fetch and the pixel read are both in bank 3** and every alternation costs a PRECHARGE and ACTIVATE. The traffic is small — a 20-window composite list is a few hundred bytes — but the stall is per crossing, not per byte. Three answers are available: give the lists a bank, interleave window parity so a backing store and the list describing it are never in the same bank, or measure and conclude it is noise. **Cheap now, awkward at G2** (→ [V.32](sec_v#v32)).
- Q66 — **The blitter reaches 16 MB, not 64, and it should be said in sheet T rather than inferred.** `DISPLAY_BASE`, `DRAW_BASE`, `LIST_BASE` and sheet U's four channel pointers are all **24-bit** ([T.48](sec_t#t48), [U.19](sec_u#u19)), and [U.16](sec_u#u16)'s partition is written as four 4 MB ranges spanning `$000000`–`$FFFFFF`. Nothing in the GUI needs more — at 8 bpp a hundred backing stores is 12 MB and the framebuffers 1.2 — but it means **the partition is a 16 MB window into the device, not a division of it**. Decide whether the address registers widen to 26 bits or the upper 48 MB is simply unaddressable by the drawing engine, and write it down either way (→ [V.33](sec_v#v33)).
- Q67 — [[!blocking]] **Are the i9 extension board's PMOD headers direct, unbuffered, bidirectional FPGA I/O?** Several Colorlight products route GPIO through unidirectional 74HC245 buffers; the module itself carries raw balls to the SODIMM, so this concerns the extension board alone. If they are buffered the 65816 data bus cannot be wired to them and [P.19](sec_p#p19)'s Stage A is dead as written. **Moot if Stage A is skipped** in favour of going straight to the carrier PCB, which is the cheaper way to close this question — continuity meter on arrival, or do not buy the extension board at all.
- Q68 — [[!blocking]] **Ball assignment for the prototype**: map [P.05](sec_p#p05)'s 82 signals onto specific i9 balls, respecting ECP5 bank grouping and keeping the CPU bus inside as few banks as practical. It is [E0.5](sec_p#e05) again on different silicon, with one extra constraint the target board does not have — the SODIMM's own zoning, which decides which signals may share a block at all.
- Q69 — **Clock plan for the merged die**: PLL outputs for the Helium domain, the Neon domain and the pixel clock, from the module's 25 MHz on P3, with [P.14](sec_p#p14) requiring the two core domains to be genuinely separate. Confirm the ECP5's PLL count covers it before the first module is written. The target board answers the same question with three devices and one oscillator ([B.7](sec_b#b7)); here it is one device and one oscillator, which is harder rather than easier.
- Q70 — **Measured current draw of the i9 module**, PHYs running and held in reset. [P.20](sec_p#p20) budgets ~500–700 mA held down and ~700 mA–1.1 A running, and both are unmeasured estimates. It decides whether USB-C at 3 A is comfortable or merely adequate, and it is measured at [P0](sec_p#p0) with a bench supply before the carrier schematic is finalised.
- Q71 — **R-2R ladder values and termination**, verified against a 75 Ω monitor input. 6-bit ladders need 1 % resistors and the loaded output has to be checked rather than assumed; a ladder that is right in theory and 20 % low into 75 Ω produces a picture that looks fine until someone compares two monitors.
- Q72 — **The prototype's target PHI2 on the carrier.** Stage A is fixed at 1–2 MHz by the wire-wrap ([P.19](sec_p#p19)); Stage B should run the target's 8 MHz ([B.1](sec_b#b1)), but that is an intention rather than a measurement — the shared nets have three loads on a carrier with a SODIMM connector in the path, which the target board does not have. **Every stall and fill number taken below 8 MHz is an existence proof and nothing more**, and the sheet should say which clock each measurement was taken at.
- Q73 — **Which parts of G0–G8 are unreachable without a power path.** [P.02](sec_p#p02) drops battery, charger, gauge and sequencing, so [sheet S](sec_s)'s `$FF` power block has no hardware on the carrier and everything resting on it — the battery indicator, the shutdown dialogue, the stale-telemetry behaviour of [E8](sec_p#e8)'s test list — cannot be exercised. Decide whether those are stubbed against a simulated power block on the prototype or simply deferred, and write the answer into [sheet V](sec_v)'s staging rather than discovering it at G6.
- Q74 — **A cache flush that never completes has no recovery path, and no existing watchdog covers it.** [F.11](sec_f#f11)'s watchdog bounds a cache fill and [R.17](sec_r#r17)'s bounds a debug transaction, but both work by resuming a stalled PHI2 — and the asynchronous flushes of [M.6](sec_m#m6) deliberately never stall it, so neither applies. A hung SDRAM controller leaves the kernel spinning on `BUSY` indefinitely, with no clock stopped and no fault raised. Wanted: a counter in Helium bounding `CACHE_FLUSH_FRAME` and `CACHE_FLUSH_ALL`, escalating to NMI or `ABORTB` — which also means spending one of the bits `FAULT_CAUSE` currently reserves at 15:6.
- Q75 — **Where the `COP` signature byte lands on the stack** — `PC−1` or `PC−2`, verified against the WDC manual rather than inferred. [J.3](sec_j#j3) takes the service number from the accumulator and no longer reads the byte, so nothing on the syscall path depends on the answer; a tracer, a disassembler or a static binary verifier does, and a wrong constant there produces a silent misdispatch rather than a visible failure.
- Q76 — **Shutdown has no mute step, and the power-down click was never considered.** The RC on the DAC's `XSMT` exists because a rail edge through an un-muted PCM5102A is a click in the headphones ([sheet H](sec_h)); it protects power-**up** only. [S.13](sec_s#s13) sequences an orderly shutdown all the way to `POWER_CTRL` with nothing muting the output, and the power-down click is the more audible of the two because the listener still has the headphones on. Two parts, both cheap and both needing to be decided together: **`XSMT` driven from `AUD_CTRL.MUTE`** rather than strapped, costing one Neon pin against the 99 of [W.38](sec_w#w38), and **a mute step inserted between stopping the window server and flushing the filesystem** — after the session's last sound, before the switched domain moves (→ [W.40](sec_w#w40)).
- Q77 — **The sample rate is 44,146 Hz and the question is whether to write that down or spend a part on 44,100.** No PLL setting reaches either standard rate from the board's 25 MHz oscillator: the iCE40 PLL is integer-only and **44.1 kHz needs a denominator factor of 441 = 3² × 7², 48 kHz one of 3,125 = 5⁵**, neither expressible as `(DIVR+1) ≤ 16` times a power of two ([W.11](sec_w#w11)). So the second PLL cannot help audio at all, and **audio's half of [Q54](sec_q#q54) is closed** — not weakened, as [T.9](sec_t#t9) has it. The integer divisor 73 at 32fs gives +0.104 %, **1.8 cents of pitch and 3.75 s/hour of drift**, against two exact alternatives that are both refused here: a second oscillator breaks [B.7](sec_b#b7)'s one-oscillator star, and a fractional NCO hands the DAC's internal PLL a `BCK` jittered by a full 9.7 ns clock period. **The recommendation is to accept it and publish it in `AUD_RATE`**, so no piece of software ever assumes a rate the hardware is not producing (→ [W.9](sec_w#w9), [W.12](sec_w#w12)).
- Q78 — **What happens when a fifth process wants sound.** Four hardware channels are a finite resource and there is no software mixer available to virtualise them — [W.1](sec_w#w1) is arithmetic, not preference: the 65816 has 181 cycles per stereo frame at 8 MHz and mixing four streams does not fit in them. The recommendation is the interface that admits it: **channel 3 reserved for the session's system sounds, channels 0–2 allocated to `open` in order, `ENODEV` to the fourth caller** — rather than silently folding a late arrival into somebody else's channel. What is still open is whether the window server may **preempt** an application's channel for an alert, which is a session-policy question of the same shape as [S.14](sec_s#s14)'s cancellation dialogue and belongs with [sheet V](sec_v) rather than with the driver (→ [W.44](sec_w#w44)).
- Q79 — **When the SD DMA engine is actually built, and what stands in for it until then.** [D45](sec_q#d45) specifies both paths but they are not the same size of job: the FIFO is a page of gateware, the DMA engine is an arbiter client with a coherence rule and a `CACHE_INVAL_FRAME` beside it. The staging that falls out of this is that [E6](sec_p#e6) and [P5.j](sec_p#p5j) can be reached on the FIFO alone, with the block driver already written against the DMA register names so nothing above it changes when the engine lands. What has to be decided is whether that is deliberate staging or an accident waiting to become permanent — **a FIFO-only machine cannot page**, and the swap reserve of [G.4](sec_g#g4) is the feature the whole card topology was chosen for.
- Q80 — **The SPI-SD clock, and whether the card can be driven fast enough to matter.** Every throughput figure in [sheet G](sec_g) assumes 25 MHz — the ceiling for SPI mode on a modern card, and one this board has no reason to miss — but the number is inherited rather than measured, and it is derived from Helium's core clock by an integer divisor that also has to produce ≤ 400 kHz for initialisation ([G.11](sec_g#g11)). Both divisors, the achievable rate on real cards over the '3257 mux, and the resulting per-block time belong in one place before the block driver's timeouts are written against a guess.
- Q81 — [[!blocking]] **How many expansion slots the board carries, and it is the only variable in [sheet X](sec_x) that fights [Q8](sec_q#q8).** One slot is three pins of Helium, two are four, and none is zero — every other cost in that sheet is a connector, a '573 and a '245, none of which touches the FPGA. The question is not really "how many": it is **whether a slot survives a pin budget that does not close for the interfaces the machine cannot boot without**, and the honest ordering is that SPI-SD, the UART and the debug port are all senior to it. What must not happen is the decision being taken by default at layout, because the connector and its buffers are cheap to route now and impossible to add later (→ [X.3](sec_x#x3), [Q27](sec_q#q27)).
- Q82 — **Base addresses for the slot windows and the slow-bus block within bank `$FF`** — `$FF:7E00` and `$FF:7F00` for the slots is a proposal chosen for being at the top of Helium's half, and the slow bus has no proposal at all. Both are part of [Q22](sec_q#q22) rather than separate from it, and they are the two allocations in that bank that can still be moved for nothing (→ [X.7](sec_x#x7)).
- Q83 — **The aperture's frame range and, more importantly, its size.** [X.8](sec_x#x8) proposes frames `$F000`–`$F1FF` and `$F200`–`$F3FF` out of the half [F.10](sec_f#f10) leaves unpopulated; the range is arbitrary and the **1 MB size is not** — it is exactly what A0–A19 reaches, and widening it means new pins on a budget that has none. Two things to settle with it: whether the range should sit at the very top of the frame space so that a future second SDRAM device could still grow contiguously beneath it, and whether the kernel's frame allocator needs to know the range exists at all or simply never receives those numbers (→ [Q81](sec_q#q81)).
- Q84 — **Ratifying the card contract, which is the part of [sheet X](sec_x) that outlives the project's own hardware.** `SLOT_ID`, `SLOT_CLASS` and `SLOT_STATUS` at fixed offsets are what make enumeration and interrupt demultiplexing cost nothing ([X.13](sec_x#x13), [X.14](sec_x#x14)) — but a class numbering that is invented one card at a time becomes unfixable, and the obligations of [X.20](sec_x#x20) are a promise made to people who will never read this document. Wanted: the class list, and the contract restated on a single page that ships with the connector rather than inside a synthesis document.
- Q85 — **The slow bus is specified as a mechanism and not yet as a register file.** [D49](sec_q#d49) settles the shape — mirrors refreshed by a sequencer, [S.16](sec_s#s16)'s staging → live → shadow, a raw-transaction escape for initialisation — and leaves open how many windows there are, how large each may be, the descriptor encoding, what the sequencer does when several windows come due in the same pass, and whether the raw escape is enough to bring up every device this tier is likely to host. The map in [sheet X](sec_x) is a proposal at that resolution and is marked as one. **Nothing depends on it yet**, which is the argument for writing it against a real second tenant rather than against an imagined one (→ [X.28](sec_x#x28)).
- Q86 — **Where the expansion tiers land in the E-series, and the answer should be "late".** Neither is required by a milestone: [E6](sec_p#e6) boots without them and [E8](sec_p#e8) is a laptop without them. What is required *now* is schematic space and reserved pins before layout ([E1.3](sec_p#e13)), which is the classic shape of a decision that gets taken by omission. The staging that suggests itself is the slow bus first — it is a sequencer and a table, and the RTC is a real tenant — with the parallel slot's gateware after the machine has something to plug into it, and both entered on [sheet P](sec_p) rather than left in this sheet (→ [X.30](sec_x#x30)).
